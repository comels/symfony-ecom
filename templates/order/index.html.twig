{% extends 'base.html.twig' %}

{% block title %}Almeko - Valider ma commande
{% endblock %}

{% block content %}
	<h2 class="mb-3 text-secondary font-weight-bold">Je passe ma commande</h2>
	<hr>
	<div class="row">
		<div class="col-md-6">
			{% set formHtml %}
				{{ form_start(form, {action:path('app_order_recap')}) }}
				{{ form_label(form.adresses, 'Choissisez votre adresse de livraison')}}
				<a href="{{path('app_address_add')}}">Ajouter une nouvelle adresse</a>

				{{ form_end(form) }}
			{% endset %}
			{{ formHtml | replace({"br" : "<br/>"}) | raw }}
		</div>
		<div class="col-md-6">
			<p class=" font-weight-bold text-center">Récapitulatif de ma commande<p/><br>
				{% for product in cart %}
					<div class="row">
						<div class="col-2"><img src="/uploads/{{product.product.illustration}}" alt="{{product.product.name}}" height="75px"></div>
						<div class="col-8 my-auto">{{product.product.name}}</div>
						<div class="col-2 my-auto">x
							{{product.quantity}}</div>
					</div>
				{% endfor %}
			</p>
		</div>
	</div>
{% endblock %}
